<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             xmlns:viewModels="clr-namespace:CoronavirusTracker.ViewModels"
             x:Class="CoronavirusTracker.Views.DetailsPage">

    <ListView ItemsSource="{Binding Stats}" x:Name="StatsForCountry">
        <ListView.ItemTemplate>
            <DataTemplate>
                <TextCell Text="{Binding City}"
                              Detail="{Binding Confirmed}" />
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</ContentPage>